// Path constants — components never concatenate URLs.
export const PATHS = Object.freeze({
  signIn: '/',
  setPassword: '/set-password',
  home: '/home',
  notes: '/notes',
  noAccess: '/no-access',
})
