node_modules
dist
coverage
package-lock.json
migrations/*.sql

# Email templates are CONTENT, not source, and Prettier is wrong about them:
# it parses *.hbs with its glimmer parser (Ember's flavour of Handlebars),
# which cannot read a partial block — `{{#> emailLayout}}` and
# `{{#*inline "subject"}}` are both syntax errors to it.
src/templates/email/
