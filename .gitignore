node_modules/
dist/
coverage/

# Per-machine environment. Committed examples end in .example; anything a
# developer fills in stays local.
.env
.env.*
!.env.example
!.env.dev.example
*.local

# The real mail relay secret (compose loads it with required:false so its
# absence is normal). See backend/mail.dev.env for the committed dev sink.
backend/.env.mail

# Editor / OS noise
.DS_Store
.vscode/
.idea/

# Claude Code session-local config
.claude/settings.local.json
